import styled from "styled-components";

const ProductCateogeryText = styled.p`
  font-size: 1.2rem;
  color: var(--color-zinc-400);
  margin-bottom: 1.2rem;
  align-self: flex-start;
`;

export default ProductCateogeryText;
